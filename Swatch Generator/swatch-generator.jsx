#target Illustrator#targetengine 'main'var docRef = app.activeDocument;function CreateSwatches() {	var swatchWidth = 200,		swatchHeight = 100,		swatchPadding = 10;    // loop through all the spot "global" swatches in the document	for (var i = 0; i < docRef.spots.length; i++) {		// if (docRef.spots[i].name !== "[Registration]") {		if (i == 1) {		    var currSwatch = docRef.spots[i],		    	color = currSwatch.color,		    	currRGB = ("R: " + color.red + ", G: " + color.green + ", B: " + color.blue),				swatch = docRef.pathItems.rectangle(-swatchPadding, swatchPadding, 200, 100),				swatchBounds = swatch.geometricBounds;				text = docRef.textFrames.pointText([0,0]);			swatch.fillColor = currSwatch.color;			swatch.stroked = false;			alert (swatchBounds);			text.contents = currSwatch.name + "\n" + currRGB;			text.position = new Array (((swatchBounds[2]-swatchBounds[0])/2 - text.width/2) + swatchPadding, ((swatchBounds[3]-swatchBounds[1])/2  + text.height/2) - swatchPadding);		}	}}CreateSwatches();
#target Illustrator#targetengine 'main'var docRef = app.activeDocument;function CreateSwatches() {	var swatchWidth = 200,		swatchHeight = 100,		swatchPadding = 10,		offsetX = 0,		offsetY = 0;    // loop through all the spot "global" swatches in the document	for (var i = 0; i < docRef.spots.length; i++) {		if (docRef.spots[i].name !== "[Registration]") {			// if (i ==2) {		    var currSwatch = docRef.spots[i],		    	color = currSwatch.color,		    	currRGB = ("R: " + color.red + ", G: " + color.green + ", B: " + color.blue),		    	swatchPosY = ((swatchHeight + swatchPadding) * (i-1) + swatchPadding);	// -1 is to account for the [Registration] swatch 				swatch = docRef.pathItems.rectangle(-swatchPosY, swatchPadding, 200, 100),				swatchBounds = swatch.geometricBounds;				text = docRef.textFrames.pointText([0,0]);			swatch.fillColor = currSwatch.color;			swatch.stroked = false;			// alert ("swatch bounds: " + swatchBounds);			var textOffY = (((swatchBounds[3]-swatchBounds[1])/2) + swatchBounds[1]) + text.height;			// alert ("text off y: " + textOffY);			text.contents = currSwatch.name + "\n" + currRGB;			text.position = new Array (((swatchBounds[2]-swatchBounds[0])/2 - text.width/2) + swatchPadding, textOffY);			alert (text.position);		}	}}CreateSwatches();/*				[0]xMin,	[1]yMin,	[2]xMax,	[3]yMaxswatch bounds: 	10,			-120,		210,		-220		// vertical center of rectoffsetY = (((yMax-yMin)/2) + yMin))*/